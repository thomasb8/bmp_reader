# bmp_reader

This simple web app renders your .bmp files in a HTML canvas manually & displays some information about them.

To run:
  1. Clone this repository
  2. Install dart if you don't have it already. (https://webdev.dartlang.org/tools/sdk#install)
  3. run 'pub get'
  4. run 'webdev serve'
